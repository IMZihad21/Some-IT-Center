import React from 'react';
import { useSpring, animated } from 'react-spring'

const Banner = () => {
    return (
        <div className="bg-azure rounded-xl shadow-md px-4 py-20 m-6 shadow-lg">
            <h1 className="text-white text-3xl font-bold pr-36">Grow your skills to build and manage applications in the cloud, on-premises, and at the edge.</h1>
        </div>
    );
};

export default Banner;